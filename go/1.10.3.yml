platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: "1.10.3"

inputs:
- name: source
- name: go-tasks

outputs:
- name: executable


params:
  entrypoint:
  entrypointPath:
  command:
  directory:

run:
  path: sh
  args: 
  - -exc
  - |
    chmod +x go-tasks/tasks.sh
    ./go-tasks/tasks.sh