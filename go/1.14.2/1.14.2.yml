platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: "1.14.2"

inputs:
- name: source
- name: go-tasks

outputs:
- name: executable


params:
  entrypoint:
  entrypointPath:
  command:
  directory:
  organization:
  proejct:

run:
  path: sh
  args: 
  - -exc
  - |
    chmod +x go-tasks/go/tasks.sh
    ./go-tasks/go/1.14.2/tasks.sh
