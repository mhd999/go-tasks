platform: linux

image_resource:
  type: docker-image
  source: {repository: node, tag: "8-alpine" }

inputs:
- name: source
- name: go-tasks

outputs:
- name: executable


params:
  command:
  directory:

run:
  path: sh
  args: 
  - -exc
  - |
    chmod +x go-tasks/node/tasks.sh
    ./go-tasks/node/tasks.sh